#!/bin/sh

set -e

psql -h postgres_server -U postgres -c "CREATE USER $POSTGRES_DB_USER WITH PASSWORD '$POSTGRES_PASSWORD';
CREATE DATABASE $POSTGRES_DB;
GRANT ALL PRIVILEGES ON DATABASE $POSTGRES_DB TO $POSTGRES_DB_USER;"